FROM python:3.12-slim

WORKDIR /app

# Install system dependencies required by Python packages (e.g. PyMuPDF)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PORT=8080

EXPOSE 8080

CMD exec gunicorn \
    --bind :${PORT} \
    --workers 1 \
    --threads 8 \
    --timeout 180 \
    app:app